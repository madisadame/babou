import type { TranslationKey } from './index';

// Dictionnaire shimaoré. VIDE pour l'instant : chaque clé absente retombe
// automatiquement sur le français (voir use-translation). À compléter avec
// les traductions fournies par l'utilisateur — il suffit d'ajouter les
// entrées ici, sans toucher au reste de l'application.
//
// Exemple une fois traduit :
//   'library.title': '…',
export const shimaore: Partial<Record<TranslationKey, string>> = {
  // À compléter.
};
